[defaults]
inventory = ./inventory.ini
host_key_checking = False
# Lab convenience: don't prompt on first connect. In prod, manage known_hosts properly.

[privilege_escalation]
become = True
become_method = sudo
